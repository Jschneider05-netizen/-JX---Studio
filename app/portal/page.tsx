"use client";import {useEffect,useState} from "react";
export default function Portal(){const [data,setData]=useState<any>(null),[error,setError]=useState("");const token=typeof window!=="undefined"?new URLSearchParams(location.search).get("token")||"":"";useEffect(()=>{if(token)fetch(`/api/portal/project?token=${encodeURIComponent(token)}`).then(async r=>{const d:any=await r.json();if(!r.ok)throw new Error(d?.error||"Portal konnte nicht geladen werden");setData(d)}).catch((e:unknown)=>setError(e instanceof Error?e.message:"Portal konnte nicht geladen werden"));},[token]);const act=async(action:string)=>{const r=await fetch(`/api/portal/project?token=${encodeURIComponent(token)}`,{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({action})});if(r.ok)location.reload()};if(error||!token)return <main className="page-main"><section className="page-intro"><h1>Kundenportal</h1><p>{error||"Öffne deinen persönlichen Projekt-Link."}</p></section></main>;if(!data)return <main className="page-main"><section className="page-intro"><p>Projekt wird geladen …</p></section></main>;const p=data.project;return <main className="page-main"><section className="page-intro"><span className="eyebrow">JX Kundenportal</span><h1>{p.name}</h1><p>Status: <b>{p.status}</b> · Version {p.version} · Freigabe: {p.approvalStatus}</p></section><section className="admin-orders"><h2>Projektfreigabe</h2><article><div><b>Aktueller Stand</b><small>Prüfe deine Vorschau und gib den Stand frei oder fordere Änderungen an.</small></div><button onClick={()=>act("approve")}>Freigeben</button><button onClick={()=>act("changes")}>Änderungen nötig</button></article></section><section className="admin-orders"><h2>Rechnungen</h2>{data.invoices.length?data.invoices.map((i:any)=><article key={i.id}><div><b>{(i.amount/100).toLocaleString("de-DE",{style:"currency",currency:"EUR"})}</b><small>{i.status}</small></div>{i.hostedUrl&&<a href={i.hostedUrl} target="_blank">Öffnen</a>}</article>):<p>Noch keine Rechnungen vorhanden.</p>}</section></main>}
